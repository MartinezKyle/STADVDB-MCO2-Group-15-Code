<html>
    <head>
        <title>MCO2 Transactions</title>
        <link rel="stylesheet" href="css\style.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    </head>
    <body>
        <div>
            <h1 style="text-align: center;">MCO2 Transaction Management </h1><a href="/add-movie" >
            <button id="add-movie" style="align: right; margin-right: auto; margin-left: auto; size: 15px; position: absolute; top: 10; right: 30"> Add Movie</button></a>
        </div>
        <br>
        <table>
            <thead>
                <tr>
                    <th id="title">Title</th>
                    <th id="year">Year</th>
                    <th id="rank">Rank</th>
                    <th id="Actions">Actions</th>
                </tr>
            </thead>
                <tbody>
                {{#each movies}}
                    <tr>
                        <td id="movie_name">{{name}}</td>
                        <td>{{year}}</td>
                        <td>{{rank}}</td>
                        <td>
                        <a href="/edit-movie/{{id}}-{{year}}"><button id="edit-button">Edit</button></a>
                        <a href="/delete-movie/{{id}}-{{year}}-{{name}}-{{rank}}"><button id="delete-button">Delete</button></a>
                        </td>
                    </tr>
                {{else}}
                    <p>No movies to display</p>
                {{/each}}
                </tbody>
        </table>
        <br>
        {{#pagination currentPage pageCount size}}
            <div class="pagination">
            {{#unless startFromFirstPage}}
                <a href="/?page=1" id="paginate_no">&lt;</a>
            {{/unless}}
            
            {{#each pages}}
                {{#if isCurrent}}
                <a href="/?page={{page}}" id="paginate_no">[{{page}}]</a>
                {{/if}}
                {{#unless isCurrent}}
                <a href="/?page={{page}}" id="paginate_no">{{page}}</a>
                {{/unless}}
            {{/each}}
            
            {{#unless endAtLastPage}}
                <a href="/?page={{lastPage}}" id="endPage">&gt;</a>
            {{/unless}}
            </div>
        {{/pagination}}
        
        
    </body>
    
</html>
<script>
    $(document).ready(function(){
        var page;
        $('#endPage').click(function(){
            $()
        });
    })
</script>


