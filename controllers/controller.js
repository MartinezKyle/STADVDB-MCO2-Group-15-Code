const controller = {
	//-----------------------Handlebars Routing----------------------------//
    	getIndex: function (req, res) {
        res.render("index");
    }
};

module.exports = controller;
